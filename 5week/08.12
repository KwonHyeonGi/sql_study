## 08.12 sql 학습
## Union, Intersect, except 

## union 연산자 
  : 두 개 이상의 select 결과를 합치는 역할
   
    SELECT product_name FROM orders UNION SELECT product_name FROM new_orders;
    중복 데이터는 제거 해줌

    SELECT product_name FROM orders UNION ALL SELECT product_name FROM new_orders;
    중복 데이터도 나옴

## Intersect 연산자
    SELECT product_name FROM orders INTERSECT SELECT product_name FROM new_orders;
  
## expect 연산자  
   SELECT product_name FROM orders EXCEPT SELECT product_name FROM new_orders;
   두번째 쿼리에 존재하는 제외한 나머지가 나옴 서로 있는 데이터만 나옴


## Case when 문
   CASE WHEN 문은 SELECT 절에서 조건에 따라 다른 값을 반환할 때 사용함.

ex) price가 100만 원 이상이면 '고가', 5만 원 이상이면 '중가', 그 외는 '저가'로 분류합니다.

  select 
    product_name,
    price,
      case
        when price >= 1000000 then '고가'
        when price >= 50000 then '중가'
        else '고가'
      end as price_level
  from orders;

  를 통해서 가능.
